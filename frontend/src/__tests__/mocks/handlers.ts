import { http, HttpResponse } from 'msw';

export const handlers = [
  // Add API mocking handlers here if needed for future features
  // Example:
  // http.get('/api/calculations', () => {
  //   return HttpResponse.json([]);
  // }),
];